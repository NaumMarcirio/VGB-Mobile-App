import { View, Text } from "react-native";
import React from "react";
import { Tabs } from "expo-router";

const ListaCompras = () => {
  return <Text> LISTA COMPRAS</Text>;
};

export default ListaCompras;
